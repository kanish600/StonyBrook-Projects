/**
 * Contains the values Button, Label, TextArea, HBox, VBox, and AnchorPane.
 * Helps program operate as desired (as Containers have no text, and Controls 
 * have no children).
 * @author Anish
 *
 */
public enum ComponentType {
	Button, Label, TextArea, HBox, VBox, AnchorPane
}

